{
  "service": {
    "name": "healthcheck",
    "namespace": "co.forsyte.healthcheck.v0",
    "description": "Shared healthcheck models for Forsyte API services",
    "models": {
      "healthcheck": {
        "description": "Healthcheck status for an API service",
        "fields": [
          {
            "name": "status",
            "type": "string",
            "description": "Health status of the service",
            "example": "healthy"
          }
        ]
      }
    },
    "resources": {
      "healthcheck": {
        "path": "",
        "operations": [
          {
            "method": "GET",
            "path": "/healthz",
            "description": "Check the overall health status of the API service",
            "responses": {
              "200": {
                "type": "healthcheck"
              }
            }
          },
          {
            "method": "GET",
            "path": "/readyz",
            "description": "Check if the API service is ready to handle requests",
            "responses": {
              "200": {
                "type": "healthcheck"
              }
            }
          }
        ]
      }
    },
    "organization": {
      "key": "forsyte"
    },
    "application": {
      "key": "healthcheck"
    },
    "version": "0.0.1"
  },
  "imported_services": [],
  "attributes": []
}